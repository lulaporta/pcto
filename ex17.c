#include <stdio.h>

int main ()
{
    int numero = 1;
    int numero di numeri = 0;
    int somma = 0;
    while (a >= 0)
    {
        printf("inserisci numero\n");
        scanf("%d", &num);
        numero di numeri= numero di numeri + 1;
    }
} 

